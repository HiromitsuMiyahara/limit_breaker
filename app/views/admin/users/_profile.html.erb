<%= image_tag user.get_profile_image(100,100), class: "rounded-circle" %>
  <h2><%= user.name %></h2>
    <p><%= user.body %></p>
    <% unless user.guest_user? %>
    <p>フォロー数:<%= link_to user.followings.count, admin_user_followings_path(user) %></p>
    <p>フォロワー数:<%= link_to user.followers.count, admin_user_followers_path(user) %></p>
    <% end %>
    <p>性別: <%= I18n.t("enums.user.gender.#{user.gender}") %></p>
    <p>体重: <%= user.weight %> kg</p>
    <p>身長: <%= user.height %> cm</p>
    <p>誕生年: <%= user.birth_year %> 年</p>
    <p>トレーニング歴: <%= user.experience %> 年</p>
    <%= link_to 'プロフィール編集', edit_admin_user_path(user), class: 'btn btn-primary' %>
    <div class="mt-2">
      <%= link_to "いいねした投稿", favorites_admin_user_path(user) %>
    </div>

